<%= form_for(visit) do |f| %>
  <% if visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
      <% visit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :patient_id, :value => @patient_id %>
  <%= f.label :visitdate %>
  <%= f.date_field :visitdate %><br />
  <%= f.collection_select(:staff_id, Staff.all, :id, :firstname) %><br />
  <%= f.collection_select(:service_id, Service.all, :id, :description) %><br />

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
